---
layout: base.njk
title: Статии
description: Правни новини и анализи от адвокат Божидар Георгиев.
permalink: /statii/index.html
---

<section class="section-padding">
  <div class="container-custom">
    <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-neutral-100 mb-6">Статии</h1>
    
    <div class="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {% for item in statiiList %}
        <a href="{{ ('/statii/' ~ item.slug ~ '/') | url }}" class="group flex flex-col rounded-2xl border border-white/10 bg-neutral-900/35 overflow-hidden shadow-lg hover:bg-neutral-900/45 transition">
          <div class="aspect-[16/9] w-full overflow-hidden bg-neutral-900">
            <img src="{{ item.hero.image | url }}" alt="{{ item.title }}" class="w-full h-full object-cover opacity-90 group-hover:opacity-100 transition">
          </div>
          <div class="p-6 flex flex-col flex-1">
            <div class="text-xl font-semibold text-neutral-100 mb-3">
              {{ item.title }}
            </div>
            <div class="text-sm text-neutral-400 leading-relaxed flex-1">
              {{ item.content.shortDescription }}
            </div>
            <div class="mt-4 pt-4 border-t border-white/10 flex items-center justify-between">
              <span class="text-sm text-brand-accent">Прочети повече</span>
              <span class="text-brand-accent text-lg transition group-hover:translate-x-0.5">→</span>
            </div>
          </div>
        </a>
      {% endfor %}
    </div>
  </div>
</section>

