---
layout: base.njk
title: Контакти
description: Свържете се с адвокат Божидар Георгиев за консултация и правна помощ.
---

<section class="section-padding">
  <div class="container-custom">
    <div class="max-w-3xl">
      <h1 class="text-3xl md:text-4xl lg:text-5xl font-semibold text-neutral-100">Контакти</h1>
      <p class="mt-3 text-neutral-400">
        Имате въпрос или желаете консултация? Свържете се с мен чрез телефон, имейл или изпратете запитване през формата.
      </p>
    </div>

    {# Row 1: contacts + map #}
    <div class="mt-10 grid gap-6 lg:grid-cols-2 lg:items-stretch">
      <div class="rounded-2xl border border-white/10 bg-neutral-900/35 p-6 shadow-[0px_4px_18px_0px_rgba(0,0,0,0.35)]">
        <div class="text-sm font-semibold text-neutral-100">Контакти</div>

        <div class="mt-4 space-y-4 text-sm text-neutral-300">
          {% if site.contact.phones %}
            {% for phone in site.contact.phones %}
              <div class="flex items-start gap-3">
                <div class="w-10 h-10 rounded-lg flex items-center justify-center border border-white/10" style="background-color: rgba(222, 173, 121, 0.2);">
                  <img src="{{ '/assets/images/telephone.png' | url }}" alt="Телефон" class="w-5 h-5 object-contain">
                </div>
                <div class="min-w-0">
                  <div class="text-neutral-400 text-xs">Телефон</div>
                  <a href="tel:{{ phone.tel }}" class="text-neutral-100 hover:text-white transition underline underline-offset-2">
                    {{ phone.display | default(phone.tel) }}
                  </a>
                </div>
              </div>
            {% endfor %}
          {% endif %}

          {% if site.contact.email and site.contact.email.mailto %}
            <div class="flex items-start gap-3">
              <div class="w-10 h-10 rounded-lg flex items-center justify-center border border-white/10" style="background-color: rgba(222, 173, 121, 0.2);">
                <img src="{{ '/assets/images/email.png' | url }}" alt="Имейл" class="w-5 h-5 object-contain">
              </div>
              <div class="min-w-0">
                <div class="text-neutral-400 text-xs">Имейл</div>
                <a href="mailto:{{ site.contact.email.mailto }}" class="text-neutral-100 hover:text-white transition underline underline-offset-2 break-all">
                  {{ site.contact.email.display | default(site.contact.email.mailto) }}
                </a>
              </div>
            </div>
          {% endif %}

          {% if site.contact.address %}
            <div class="flex items-start gap-3">
              <div class="w-10 h-10 rounded-lg flex items-center justify-center border border-white/10" style="background-color: rgba(222, 173, 121, 0.2);">
                <img src="{{ '/assets/images/gps.png' | url }}" alt="Адрес" class="w-5 h-5 object-contain">
              </div>
              <div class="min-w-0">
                <div class="text-neutral-400 text-xs">Адрес</div>
                <div class="text-neutral-100">{{ site.contact.address }}</div>
              </div>
            </div>
          {% endif %}

          {% if site.contact.officeHours %}
            <div class="flex items-start gap-3">
              <div class="w-10 h-10 rounded-lg flex items-center justify-center border border-white/10" style="background-color: rgba(222, 173, 121, 0.2);">
                <img src="{{ '/assets/images/working-time.png' | url }}" alt="Работно време" class="w-5 h-5 object-contain">
              </div>
              <div class="min-w-0">
                <div class="text-neutral-400 text-xs">Работно време</div>
                <div class="text-neutral-100">{{ site.contact.officeHours }}</div>
              </div>
            </div>
          {% endif %}

          {% if site.contact.callHoursNote %}
            <div class="pt-4 border-t border-white/10 text-xs text-neutral-400">
              {{ site.contact.callHoursNote }}
            </div>
          {% endif %}
        </div>
      </div>

      <div class="rounded-2xl border border-white/10 bg-neutral-900/35 p-6 shadow-[0px_4px_18px_0px_rgba(0,0,0,0.35)]">
        <div class="text-sm font-semibold text-neutral-100">Локация</div>
        <div class="mt-4 overflow-hidden rounded-xl border border-white/10">
          <iframe
            src="{{ site.contact.mapEmbedUrl }}"
            width="100%"
            height="350"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            class="w-full h-[320px] lg:h-[280px]"
          ></iframe>
        </div>
      </div>
    </div>

    {# Row 2: form #}
    <div class="mt-6 rounded-2xl border border-white/10 bg-neutral-900/35 p-6 shadow-[0px_4px_18px_0px_rgba(0,0,0,0.35)]">
      <div class="text-sm font-semibold text-neutral-100">Форма за контакт</div>
      <p class="mt-2 text-sm text-neutral-400">Изпратете запитване и ще се свържа с вас при първа възможност.</p>

      <form action="https://api.web3forms.com/submit" method="POST" class="mt-6 space-y-4">
        {% if mailKey %}
          <input type="hidden" name="access_key" value="{{ mailKey }}">
        {% endif %}
        <input type="hidden" name="subject" value="Ново запитване от сайта">
        <input type="hidden" name="redirect" value="{{ '/blagodarya/' | url }}">
        <input type="checkbox" name="botcheck" class="hidden" tabindex="-1" autocomplete="off">

        <div class="grid gap-4 md:grid-cols-2">
          <div>
            <label for="contact-name" class="text-xs text-neutral-400">Име и фамилия</label>
            <input id="contact-name" name="name" type="text" class="mt-2 w-full rounded-xl border border-white/10 bg-neutral-950/40 px-4 py-3 text-neutral-100 placeholder:text-neutral-500 focus:outline-none focus:ring-2 focus:ring-brand-secondary/30" required>
          </div>
          <div>
            <label for="contact-email" class="text-xs text-neutral-400">Имейл</label>
            <input id="contact-email" name="email" type="email" class="mt-2 w-full rounded-xl border border-white/10 bg-neutral-950/40 px-4 py-3 text-neutral-100 placeholder:text-neutral-500 focus:outline-none focus:ring-2 focus:ring-brand-secondary/30" required>
          </div>
        </div>

        <div>
          <label for="contact-message" class="text-xs text-neutral-400">Вашето запитване</label>
          <textarea id="contact-message" name="message" rows="7" class="mt-2 w-full rounded-xl border border-white/10 bg-neutral-950/40 px-4 py-3 text-neutral-100 placeholder:text-neutral-500 focus:outline-none focus:ring-2 focus:ring-brand-secondary/30 resize-none" required></textarea>
        </div>

        <div class="pt-2">
          <button type="submit" class="btn-brand w-full md:w-auto" {% if not mailKey %}disabled{% endif %}>
            Изпрати
          </button>
        </div>
        {% if not mailKey %}
          <p class="text-xs text-neutral-400 mt-2">* Формата ще бъде активна след добавяне на API ключ</p>
        {% endif %}
      </form>
    </div>
  </div>
</section>
